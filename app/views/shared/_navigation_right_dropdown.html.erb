<div style="margin-right: 55px;">
           <ul class="nav navbar-nav navbar-right"> 
            
              
                <% if member_signed_in? %>
                <li><%= image_tag current_member.avatar.url(:thumb), class: "profile-thumb" %></li>
                <li><%= link_to current_member.name, member_path(current_member.id)%>
                
              </li>

            <li><div>

              <ul class="nav navbar-nav navbar-user"> 
                       
              <li class="dropdown">
              
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">

                  

                <span class="caret"></span>

              </a>
              <ul class="dropdown-menu" role="menu">
                <li><%= link_to "View Profile", profile_path(current_member.id), :member_id => current_member.id %></li>
                 <% if current_member.try(:admin?) %>
                    <li><%= link_to('Admin Panel', members_path)%></li>
                 <% end %>
                 <% if current_member.try(:editor?) %>
                    <li><%= link_to('Editor Panel', editors_index_path)%></li>
                  <% end %>

                <li><%= link_to('Sign Out', destroy_member_session_path, :method => :delete)%></li>
              </ul>
            </li>
            
          </ul>
          </div>
          </li>
    <% else %>
     <li><%=link_to "Log In", new_member_session_path %></li>
            <li><%=link_to 'Sign Up', new_member_registration_path %></li>
            <%end%>
           </div>
           
          </ul>
        </div>



